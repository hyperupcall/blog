# Documentation for `build.js`

Wedding myself to a different static site generator every several years is a poor use of my time and limits the degree to which I can customize my website. `build.js` solves that because it only does what I need and its customization is not constrained by the (different) APIs available within a plugin boundary.

Some properties:

-
